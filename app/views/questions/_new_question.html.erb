<% if policy(Question).new? %>
  <%= link_to t('.new', :default => t("helpers.links.Ask_a_New_Question!")),
              new_question_path,
              :class => 'btn btn-primary' %>
<% end %>              
