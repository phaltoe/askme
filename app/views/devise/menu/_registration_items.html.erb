<% if user_signed_in? %>
  <li>
    <%= link_to "Hello, #{current_user.name}!", user_path(current_user) %>
  </li>
<% else %>
  <li>
  <%= link_to('Sign Up!', new_user_registration_path)  %>
  </li>
<% end %>